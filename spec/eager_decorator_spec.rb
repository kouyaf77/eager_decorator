require "spec_helper"

describe EagerDecorator do
  it "has a version number" do
    expect(EagerDecorator::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
